// Title: Set Mismatch
            // Difficulty: Easy
            // Language: Java
            // Link: https://leetcode.com/problems/set-mismatch/

            if (freq[i] == 0) {
        }
        // Find duplicate and missing

        for (int i = 1; i <= n; i++) {
            if (freq[i] == 2) {
            }
                ans[1] = i; // missing
                ans[0] = i; // duplicate
            }
        }
            freq[num]++;
        for (int num : nums) {
        // Count frequency

        int[] ans = new int[2];
